# UE-Agent 项目专属上下文

> 本文件是可随项目交付的长期上下文，供接手人员或 AI 继续开发时阅读。
> 只记录 UE-Agent 项目本身的产品、数据和工程约定，不包含用户画像、账号信息、访问令牌或其他养老业务项目资料。

## 1. 产品定位

UE-Agent 是一个面向长护险/养老业务的城市成本测算网站 Demo。核心目标不是复刻 Excel 的操作界面，而是把 Excel 中已经确认的测算口径转成可追溯、可对比、可视化的城市测算结果。

产品主线：

1. 新增一个城市，形成一个独立的城市测算项目。
2. 系统根据固定模型和城市参数计算成本、收入、盈亏和关键运营指标。
3. 单城市页面允许查看和修改可编辑参数。
4. 总览页面把全部城市的已保存结果映射到仪表盘，支持全局汇总、城市切换和城市对比。
5. 政策资料页面按城市保存政策来源、原文文件、解析结果和 AI/浏览器兜底任务，并为测算参数提供可采纳的建议值。

当前阶段是 Demo/交付版本：暂不做登录页，所有人可查看测算结果；数据采用本地 SQLite 和本地文件目录，便于一键移植和继续开发。

## 2. 当前工程组成

- `apps/web/`：Next.js 前端，负责总览、城市测算、政策资料、城市对比和参数编辑。
- `apps/api/`：FastAPI 后端，负责城市项目、参数、计算结果、政策来源、文件、抓取任务和导出接口。
- `apps/api/data/`：本地 SQLite 数据库及本地业务数据。
- `apps/api/policy_files/`：政策原始文件和导出相关文件。
- `apps/api/raw_sources/`：政策抓取后保存的原始正文/文件快照。
- `packages/`：前端共享 UI、类型和配置。
- `.workbuddy/skills/`：项目专属的 WorkBuddy Skill，当前主要包含政策资料检索/解析和城市初始化规则。
- `.workbuddy/automations/`：可复制的 WorkBuddy 定时任务模板；模板不包含任何账号密钥。
- `docs/`：需求、开发、接口、部署和交付文档。
- `scripts/`：本地启动、验证、数据初始化和交付打包脚本。
- `AGENTS.md`：项目开发约束；继续开发前必须先阅读。

主要技术路线：Next.js + TypeScript + Tailwind/shadcn 风格 UI + FastAPI + SQLite + 本地文件存储。前端图表应优先复用已有图表组件和统一的 UI token，不要新引入一套视觉体系。

## 3. 数据和测算口径

模型字段来源必须严格遵循原始 UE Excel 和已确认的项目字段设计。字段展示要区分以下语义：

- `内部填写`：用户可在网站内直接填写或修改。
- `自动爬虫`：政策资料检索/解析后产生的建议值；允许用户采纳后写入，不能静默覆盖人工已确认值。
- `公式自动`：由模型公式计算，只读展示；如需改变口径，应修改模型默认值或公式配置，不在结果填写框中直接改结果。
- `其他`：业务人员按实际情况填写，也可以留空。

前端交互约定：

- 自动爬虫的建议值在输入框中以灰色 placeholder/建议态表现；placeholder 不是实体值，不得参与计算。
- 用户点击“采纳”或明确保存后，建议值才成为参数值，并记录来源、抓取时间、版本和证据。
- 公式自动字段显示为只读，并标记“公式”；没有足够输入时显示“待补充/无法计算”，不能把缺失值当成 0。
- 原始值、建议值、已确认值、公式值、展示值要能区分；不要为了填满页面而把语义不同的字段互相替代。
- 疑似 Excel 问题和原文不确定项原样保留，并标记“待业务确认”。
- 已确认或人工覆盖的参数不得被后续抓取、AI 解析或重新计算静默覆盖。
- 每次结果都应能追溯到模型版本、参数快照、数据来源和计算时间。

参数来源与城市 ID 必须保持稳定：城市项目、政策来源、抓取任务、参数建议和计算结果统一使用同一个 `cityId`，城市名称只用于展示。URL 中出现中文城市名时要正常支持 URL 编码/解码。

## 4. 政策资料与实时更新链路

固定官网链接只能作为来源线索，不能假设页面 URL 变化后仍能直接拿到最新政策。实时更新采用“系统接口 + WorkBuddy AI 检索/浏览器 + 本地落库”的协作方式：

1. 系统保存城市、政策主题、来源线索和目标字段映射。
2. WorkBuddy Skill 根据城市和主题检索最新官方政策，优先官方政府/医保/统计来源，并保存标题、发布日期、来源链接、正文/文件快照和检索说明。
3. WorkBuddy 将结构化候选值提交到系统接口；系统负责校验、去重、版本比较、证据保存和建议值入库。
4. 只有正文首次抓取或正文 hash 发生变化时，才进入 AI 字段抽取；未变化的来源直接跳过。
5. 用户在城市参数页查看灰色建议值，明确“采纳”后才影响测算。
6. 抓取失败不能伪装成成功，也不能用空值覆盖旧数据；应记录失败原因、重试次数、下次重试时间和可执行的 WorkBuddy 浏览器兜底任务。

抓取兜底规则：

- 普通 HTTP 抓取失败时，先做有限重试和响应类型判断，再判断是否需要浏览器兜底。
- 对 HTTP 412、TLS/国密证书、WAF/验证码、JS 动态页面、超大 PDF、扫描件和超时，要进入明确的浏览器/文件解析流程，而不是无限重试。
- PDF 或正文过大时应下载保存后分段解析，先提取目录/标题/关键词相关页，再让 AI 对相关片段抽取；保留原文件，禁止只保存摘要。
- 浏览器兜底产物要回传系统，形成可下载的 artifact，并关联到原来源、城市和任务。
- 不能把 WorkBuddy 私有登录态、Cookie、Token 或本机路径写入项目代码、配置或交付包。

## 5. 页面和交互约定

核心导航保持简单：

- 总览：全局城市仪表盘，默认展示全部城市，也可切换到单城市。
- 城市测算：城市列表和单城市参数/结果页；重点是编辑参数、运行测算、查看成本分析。
- 政策资料：全局政策来源总览和单城市政策资料/抓取任务页。
- 城市对比：选择城市和指标进行横向对比。

工作台和 U1 城市选址不作为核心导航入口。若历史页面仍存在，优先保持兼容，不再扩大功能范围。

视觉与组件约定：

- 保持现有浅色、克制、数据工作台风格；主色使用现有 teal/semantic token，不在页面内写一套新颜色。
- 按钮使用统一高度、圆角、字重和 hover/focus 状态；中文按钮文案简短，避免图标和文字挤压或换行。
- 图标优先使用项目已有图标库；不要把 emoji 作为正式界面图标。
- 表格、卡片、标签、表单、空状态、加载态和错误态优先复用 `packages/ui` 和已有组件。
- 所有异步操作都必须有加载、成功、失败和空数据状态；抓取任务要显示真实状态和失败原因。

## 6. 项目专属 WorkBuddy 资产

交付时需要一并查看：

- `.workbuddy/skills/policy-ai-crawler/SKILL.md`：政策检索、浏览器兜底、证据保存和接口回传约定。
- `.workbuddy/skills/policy-city-onboarding/SKILL.md`：新增城市后的数据源发现、政策初始化和参数建议约定。
- `.workbuddy/automations/policy-ai-sync.template.json`：可复制的定时同步模板；接手方需要在自己的 WorkBuddy 环境中配置执行身份和项目路径。

这些是项目资产，不依赖用户级 Skill 安装目录。交接时应把整个仓库和本地数据按交付文档复制给接手方，再由接手方在自己的 WorkBuddy 中导入 Skill/任务模板。

## 7. 开发和验证约定

首次启动可按以下顺序执行：

```bash
pnpm setup
pnpm dev:all
```

常用验证：

```bash
pnpm api:test
pnpm test
pnpm typecheck
pnpm lint
pnpm build
pnpm handoff:check
pnpm handoff:package
bash scripts/e2e-agent-flow.sh
```

如果 API 测试环境的 Python 依赖不在默认 PATH，使用项目提供的 `bash scripts/api-python.sh ...` 入口。每次改动后至少执行与改动范围匹配的检查；涉及交付包时必须检查压缩包内容，确认没有环境变量、Token、私有记忆或本机临时文件。

## 8. 交接边界：哪些记忆不能打包

本项目只交付本文件、根目录 `AGENTS.md`、前端目录约束、项目 Skill、自动化模板、源码、文档和明确纳入的演示数据。

以下内容不属于项目记忆，必须保持在项目交付包之外：

- 账号级或用户级记忆目录，例如 `.codex/memories/`。
- WorkBuddy 私有运行记忆，例如 `.workbuddy/memory/`。
- 用户画像、个人偏好、其他养老项目的业务资料和真实个人信息。
- `.env`、`.env.local`、访问令牌、Cookie、登录态、私钥和本机绝对路径。
- `.git/`、依赖目录、构建缓存、临时文件和 `.DS_Store`。

如果接手人员需要新的项目约定，应更新本文件或 `AGENTS.md`，并在交付说明中记录变更；不要把账号级记忆复制进仓库。
