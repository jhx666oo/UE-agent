node_modules/
.next/
out/
coverage/
.turbo/
*.tsbuildinfo
__pycache__/
*.py[cod]
services/api/data/
services/api/backups/
services/api/.venv/

# 发布运行时数据与临时虚拟环境。
runtime-data/
.venv-publish/

# Next.js 16 generates these workspace hints during dev.
apps/web/AGENTS.md
apps/web/CLAUDE.md
.vercel
.env*

# 本地工具与工作区数据，不进入版本库。
# 例外：项目级 skill 属于交付物，要随项目一起给别人 —— 所以只忽略 .workbuddy 下的
# 其他内容（memory/、缓存等），skills/ 必须跟踪。
# 注意必须写成 `.workbuddy/*` 而不是 `.workbuddy/` —— 后者会让 git 整个目录不进入，
# 里面的例外规则（!）就失效了。
.arts/
.workbuddy/*
!.workbuddy/skills/
!.workbuddy/skills/**
